{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>{{ post.title }}</h1>
        <img src="{{ '/uploads/' ~ post.image }}" alt="">
    </div>
    

{% endblock %}